let a = null
const names = 'chidi';
const age = 20;
const isMonday = true;

const undefinedd = undefined
// console.log(typeof undefinedd)
// console.locg(typeof a) === object

/// Variables are defined using the var, let and const keys
/// var is functional scope while let and const is block scoped.


///To access a property of an oject using bracket notation, make sure to stringify the property
const data = {
  age:49,
  name:'Enuch'
}

//console.log(data['age'])

//using dot notaion 
console.log(data.name)

// Accessing complex object/nested oject
const shoes = {
  name:'Versace',
  make: {
    news:2,
    tel:'luigi'
  }
}

let c = 20
console.log( c > 21)
if (c > 19 ){
  console.log(c)
}


